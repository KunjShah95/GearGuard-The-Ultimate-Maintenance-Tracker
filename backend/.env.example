# Database Configuration
# Use these credentials for local PostgreSQL (password is case-sensitive!)
DATABASE_URL="postgresql://postgres:password@localhost:7839/gearguard?schema=public"

# JWT Secret - Change this in production!
JWT_SECRET=""

# Server Configuration
PORT=5000
NODE_ENV=development
